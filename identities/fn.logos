
fn := type (
    input := type ?
    output := ?
    bcode := callable ?
    frame := u64 ?
    shared compile := fn () -> void ( ? )

    parse_rank = …
    associativity = left
    # Takes `(params)`, `->` and the body; the function it builds runs, and is never parsed again.
    parse = ( ? )
)
