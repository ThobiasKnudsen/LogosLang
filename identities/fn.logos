fn := type (
    parse = ( ? )
    instance = (
        shared parse_rank = …
        shared associativity = left
        shared parse = ( ? )
        shared compile := fn () -> void ( ? )
        input := type ?
        output := ?
        bcode := callable ?
        frame := u64 ?
    )
)
